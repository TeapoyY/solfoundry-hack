# 2026-04-23 日记

## 今日大事

### Gateway 崩溃（两次）

**崩溃 1**（发现时间 13:18 HKT）
- 崩溃时间：~10:11 HKT（log最后写入时间），网关恢复 13:18 HKT
- 原因：AI service overload → bounty-hub-hunter 和 ai-money-hunter 连续报错进入 backoff → Feishu delivery storm（163条消息发送到无效 user_id "heartbeat"）→ gateway 主进程阻塞 → 崩溃
- 恢复：gateway-keepalive 检测到崩溃 → 自动重启 → 13:18 HKT 恢复
- 82条消息 pending 在 delivery queue（重启后 recovery 失败，status code 400）

**崩溃 2**（发现时间 18:02 HKT）
- 崩溃时间：~17:02 HKT
- 原因：同第一次（delivery queue 积压 + Feishu 投递失败）
- 恢复：手动 `openclaw gateway restart` → 18:02 HKT 恢复
- 后续投递 recovery: 164条消息，0 recovered，82 failed

### Feishu 投递队列问题（根因）
- **症状**：163条消息永久投递失败（status code 400），目标 user_id = "heartbeat"
- **根因**：OpenClaw Feishu channel 配置了一个名为 "heartbeat" 的 contact，但该 contact 在 Feishu 系统中不存在（无效的 user_id）
- **影响**：投递失败 → gateway 不断重试 → 积压 → 主进程阻塞 → 崩溃
- **来源**：bounty-hub-hunter 的结果发送给 "heartbeat" contact（可能是 HEARTBEAT 通知的目标配置错误）
- **修复**：清除了 delivery queue 中的所有 failed 消息
- **待处理**：需确认 "heartbeat" contact 来源（是 OpenClaw 内置还是配置错误）

### polymarket-elon-monitor 停止（核心 Bug）
- **症状**：cron 状态显示 "skipped"，每次触发都立即跳过
- **根因**：jobs.json 中 polymarket job 配置了 `"sessionTarget": "main"` + `"payload": {"kind": "agentTurn", ...}`
  - OpenClaw scheduler 验证：main session job 只能有 systemEvent payload
  - 重启后 scheduler 更严格了，导致 polymarket job 被拒绝
- **修复**：jobs.json 中将 polymarket-elon-monitor 的 `sessionTarget` 从 `"main"` 改为 `"isolated"`
- **验证**：jobs.json 已更新，scheduler 下次加载后生效

### Self-Improve 工作
- [x] 清空 failed delivery queue（493个历史垃圾文件 + 82个今日失败消息）
- [x] 修复 polymarket cron：sessionTarget main → isolated
- [ ] 调查 "heartbeat" contact 来源（OpenClaw 内置还是配置问题）
- [ ] 考虑禁用 bounty-hub-hunter（browser relay 长期断开，持续产生垃圾投递）
- [ ] gateway-keepalive 应监控 delivery queue 深度

## 当前状态（19:01 HKT）
- Gateway: ✅ 运行正常
- polymarket-elon-monitor: ✅ 修复已写入 jobs.json，下次 scheduler 加载生效
- 投递队列: ✅ 已清空
- 其他 cron jobs: bounty-hub-hunter (error), ai-money-hunter (error), Daily PR Review (error)

## Elon 市场信号（最后成功运行 10:03 HKT）
- apr17-24: xtrack=93/200, BUY_NO (P_NO=100%, PM YES=85%)
- may2026: xtrack=0/800, BUY_YES (P_YES=100%, PM YES=85%, edge +15%)
- apr14-21: 已过期
